import { strict } from "assert";

function cumulative_sum(list){
    let output = [];

    for(let i=0; i<list.length; i++){
        if(i==0){
            output.push(list[i])
        }else{
                output.push(list[i-1]+ output[i])
        }
    }return output;
}

const f = n => n < 1 ? 1 : n *f(n-1);

let g = f(4)


let event = {
    name: "Hot Dog and Burger Sunday",
}


<div class='wrapper'>
<div id="id" class="elem">Text</div>

def reverse_list(e)


f('foo');
setTimeout(function(){f('bar');},0);
f('baz');

var a = str.split('dog').join('cat')